{
  "name": "nexgenai",
  "version": "1.0.0",
  "description": "NexGenAI — Campus to Career AI Operating System",
  "private": true,
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "dev": "concurrently -n \"FASTAPI,VITE\" -c \"blue,magenta\" \"backend\\venv\\Scripts\\python.exe -m uvicorn backend.app.main:app --host 127.0.0.1 --port 8000 --reload\" \"npm run dev --workspace=frontend\"",
    "build": "npm run build --workspace=frontend",
    "test": "backend\\venv\\Scripts\\python.exe -m pytest tests/test_api.py -v",
    "db:seed": "backend\\venv\\Scripts\\python.exe -m database.seeds.seed_data"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "author": "NexGenAI Team",
  "license": "ISC"
}
